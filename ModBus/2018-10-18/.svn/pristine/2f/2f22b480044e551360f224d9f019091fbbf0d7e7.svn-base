#pragma once

#include "AbstractIpcObject.h"


public ref class IpcClient : public AbstractIpcObject
{
public:
	IpcClient();
	IpcClient(int port);
	virtual ~IpcClient();

	virtual IpcMode GetIpcMode() override;

	virtual IpcType GetIpcType() override;

	/*void Start();

	void Stop();*/

private:
	//IChannel ^ mChannel;
};

